# Active Tasks

- Keep one active task per contributor.
- Record each status change in frontmatter history.
- Move files between lifecycle directories with `git mv`.
- Use `lore-codex-tasks` before every lifecycle change.
