# Archive

Archived tasks use one of these terminal statuses:

- `completed`
- `superseded`, with `by`
- `canceled`, with reason `pivot`, `obsolete`, or `duplicate`

Use `lore-codex-tasks` before archiving.
