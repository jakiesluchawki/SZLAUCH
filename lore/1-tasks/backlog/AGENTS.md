# Backlog

Backlog tasks use `status: backlog`.

Recommended tags:

- `priority-high`, `priority-medium`, `priority-low`
- `effort-small`, `effort-medium`, `effort-large`

Use `lore-codex-tasks` to promote an item to active work.
