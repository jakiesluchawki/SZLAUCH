# Architecture Decision Records

ADRs explain why a stable technical or product decision was made.

Lifecycle:

```text
proposed -> accepted -> deprecated | superseded
```

Each ADR requires an ID, title, status, deciders, relationships, and status
history. Use `_template.md`.
